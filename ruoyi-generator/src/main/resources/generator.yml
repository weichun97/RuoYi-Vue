# 代码生成
gen: 
  # 作者
  author: ruoyi
  # 默认生成包路径
  packageName: com.ruoyi.admin
  # 默认的模块
  module: test
  # 自动去除表前缀，默认是false
  autoRemovePre: true
  # 表前缀（生成类名不会包含表前缀，多个用逗号分隔）
  tablePrefix: sys_